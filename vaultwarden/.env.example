# Vaultwarden 환경 설정 예시
# 이 파일을 복사하여 .env로 저장하고 값을 설정하세요

# 외부 접속 도메인
DOMAIN=https://vault.example.com

# 관리자 패널 접근 토큰 (주석 해제 시 /admin 활성화)
# 토큰 생성: openssl rand -base64 48
# ADMIN_TOKEN=your_secure_token_here

# 가입 허용 설정 (계정 생성 후 false로 변경 권장)
SIGNUPS_ALLOWED=false
